Hostile-input coverage for the Phase C bundle validator lives here.

The MVP accepts an already-decoded manifest and file list. Archive parsing and
extraction are deliberately unsupported until bounded streaming extraction,
compression-ratio limits, and archive-specific path handling are designed.
