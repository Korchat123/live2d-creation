# P0-A Gate A report — FAIL

- Feature HEAD: `124da755a6deb61629ccf5011fa57dd4864d0cb8`
- Immutable code candidate: `25f2528bb65fe51ef6c6472c865bd860d69435aa`
- Governing contract: `standard-bust-v1/spec-0.6.0`
- Independent result: **FAIL**

This report records the completed findings from the independent Gate 7 review session. The reviewer finished the mechanical and visual review but hit its service limit before committing the report; the coordinator recorded the unchanged findings here rather than treating the interrupted handoff as a pass.

## Passed evidence

- 35/35 Node tests and 1/1 real-Chromium responsive test passed.
- The independent real-browser sweep rendered all 1,159 states: 1,122 `Needs review` and exactly 37 documented correlated `Blocked` states.
- All 48 advertised isolated endpoints were accepted.
- All 11 rejection fixtures blocked; no non-finite geometry or selector-playback mismatch was found.
- Character-bible, executable, README, UI and control contract identities matched `spec-0.6.0`.
- `custom:bounded` state identity, visible bust propagation, zero-bust topology, head, hair, ears and anti-cheating checks passed.
- `124da75` changes only the tracker over code candidate `25f2528`; there is no unreviewed code delta.

## Blocking visual defect

At desktop and especially at 390 px, the accepted body still reads as a broad sleeveless shield/container. The paired inner axilla curves look like decorative garment seams drawn over the existing outer shape; they do not define separately owned deltoid, upper-arm, underarm and torso surfaces. The silhouette therefore remains unsafe as the canonical anatomy on which hair, clothing and rigging templates would depend.

Gate A remains failed even though the automated checks pass. The next candidate must replace the seam-overlay construction with semantic torso and arm surfaces, prove their attachment and ownership in the render graph, and pass a fresh independent visual review.
