# Reference avatar source art

These SVG studies were authored in this repository from simple geometric
shapes. They use no external images, fonts, logos, characters, or references.

The palette and construction follow `docs/authoring/visual-spec.md` and
`docs/authoring/layer-spec.md`. SVG element IDs use the canonical layer names
where a study represents a runtime-visible part.
