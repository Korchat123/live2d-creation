# Minimal avatar fixture

This bundle is a deliberately small layered avatar for renderer contract and
golden-image tests. `avatar.json` is the public schema-valid manifest.
`render.json` is the Phase B renderer-spike scene description and is not a
public bundle contract.

All visible assets are original geometric SVGs authored for this repository.
Their provenance and permissions are recorded in `RIGHTS.json`.
